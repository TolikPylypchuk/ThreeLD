@model ThreeLD.Web.Models.ViewModels.NotificationsViewModel

@{
    ViewBag.Title = "My Notifications";
}

<h1>My Notifications</h1>
<div class="container">
    <div class="row">
        <div class="col">
            @foreach (var n in Model.UnreadNotifications)
            {
                <ul class="list-group">
                    <li class="list-group-item">@n.Message</li>
                </ul>
            }
        </div>
        <div class="col">
            @foreach (var n in Model.UnreadNotifications)
            {
                using (Html.BeginForm("CheckNotificationAsRead", "User", new { notificationId = n.Id }))
                {
                    <button type="submit" class="btn btn-info">
                        Check all as read
                    </button>
                }
            }
        </div>
    </div>
</div>

@foreach (var n in Model.ReadNotifications)
{
    <ul class="list-group">
        <li class="list-group-item">@n.Message</li>
    </ul>
}
